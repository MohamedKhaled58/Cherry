#pragma once
#include "AudioManager.h"

namespace Cherry {
    // Audio.h serves as a convenient include point for the audio system
    // All audio functionality is provided through AudioManager::Get()
    // AudioHandle and INVALID_AUDIO_HANDLE are defined in AudioManager.h
}